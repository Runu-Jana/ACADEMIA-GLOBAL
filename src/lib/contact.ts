/**
 * Every way a learner or a university can reach us, in one place.
 *
 * These strings surface on the marketing site, in the student dashboard, in the
 * legal pages and on the printed brochure. They used to be retyped at each call
 * site, which is how the helpdesk ended up advertising two different phone
 * numbers and an email address on a domain we no longer use. Change them here
 * and every surface follows.
 *
 * ⚠️ PLACEHOLDERS — these are not real contact details. Set them to the live
 * ones before launch: a learner who calls or emails what's below reaches nobody.
 */

/** General learner support — admissions, exams, documents, refunds. */
export const SUPPORT_EMAIL = 'support@academiaglobal.in'

/** Inbound university / institution partnership enquiries. */
export const PARTNERSHIPS_EMAIL = 'partners@academiaglobal.in'

/** Helpline, as displayed to a human. */
export const SUPPORT_PHONE = '1800-123-4567'

/** The same number, dial-ready for a `tel:` href. */
export const SUPPORT_PHONE_HREF = 'tel:18001234567'

/** When the helpline is actually staffed. */
export const SUPPORT_HOURS = 'Mon – Sat, 9AM – 8PM'

/** Registered office, for the footer and the contact page. */
export const HEAD_OFFICE_CITY = 'Karnal, Haryana'
export const HEAD_OFFICE_POSTCODE = 'India — 132001'
